CREATE TABLE IF NOT EXISTS db_usuario.rol (
    id_rol BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nombre_rol VARCHAR(30) NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS db_usuario.estado_usuario (
    id_estado_usuario BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nombre_estado VARCHAR(30) NOT NULL UNIQUE
);